hey its here
<% @bad_shifts.each do |shift| %>
<%= shift.id %>
<%= shift.created_at %>
<% client = CarePair.find(shift.care_pair_id).client%>
<% worker = CarePair.find(shift.care_pair_id).worker%>
<%= client.first_name + ' ' + client.last_name %>
<%= worker.first_name + ' ' + worker.last_name %>
<% end %>
